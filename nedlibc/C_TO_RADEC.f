	SUBROUTINE C_TO_RADEC(C,RA,DEC)
C
C	UNIT VECTOR C INTO RA & DEC
C
	REAL*4 C(3),RA,DEC
	PARAMETER (PI=3.1415927,TWOPI=6.2831853,DTR=57.29577951)
	RA = ATAN2(C(2),C(1))
	IF(RA.LT.0.) RA=RA+TWOPI
	DEC = ATAN2(C(3),SQRT(C(1)**2+C(2)**2))
	RETURN
	END
