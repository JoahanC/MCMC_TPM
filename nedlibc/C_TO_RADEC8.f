	SUBROUTINE C_TO_RADEC8(C,RA,DEC)
C
C	UNIT VECTOR C INTO RA & DEC
C
	REAL*8 C(3),RA,DEC,TWOPI
	PARAMETER (TWOPI = 6.28318530717958D0)
	RA = ATAN2(C(2),C(1))
	IF (RA.LT.0.) RA=RA+TWOPI
	DEC = ATAN2(C(3),SQRT(C(1)**2+C(2)**2))
	RETURN
	END
