	SUBROUTINE ORTH(V,W,X)
	REAL*4 V(3),W(3),X(3)
C
C	CONSTRUCT AN ORTHONORMAL TRIPLET BASED ON V
C
	B=0.
	DO 10 I=1,3
	AV=ABS(V(I))
	IF(AV.LE.B) GO TO 10
	   B=AV
	   M=I+1
10	W(I)=0.
	IF(M.EQ.4) M=1
	W(M)=1.
	CALL ORTH2(V,W,X)
	RETURN
	END
